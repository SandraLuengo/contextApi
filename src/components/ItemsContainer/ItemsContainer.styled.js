import styled from 'styled-components';
import ItemsContainer from './ItemsContainer';

export default styled(ItemsContainer).attrs({})`
    height:94vh;
    width: 100vw;
    display:flex;
    justify-content:space-around;
    align-items:center;
`;
